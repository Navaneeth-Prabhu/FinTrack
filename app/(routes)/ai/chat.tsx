import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ChatScreen from '@/screens/ai/chat.screen'

const Chat = () => {
    return (
        <ChatScreen />
    )
}

export default Chat

const styles = StyleSheet.create({})